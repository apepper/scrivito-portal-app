export const alignmentAttributes = {
  align: ['enum', { values: ['left', 'center', 'right'] }],
  textAlign: ['enum', { values: ['left', 'center', 'right'] }],
  verticalAlign: ['enum', { values: ['top', 'middle', 'bottom'] }],
} as const
